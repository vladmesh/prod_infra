# Copy to prod.ini and set your server IP in ansible_host.
[prod]
vps ansible_host=YOUR_SERVER_IP

[prod:vars]
# Use root for bootstrap, then switch to deploy user
# For bootstrap: ansible_user=root
# For site.yml: ansible_user=deploy
ansible_user=deploy

